<div class="app-container-form">
  <div class="stack-lg">
      <header class="text-center stack-sm">
        <h1 class="heading-page">Add Workout</h1>
        <% if params[:date].present? %>
          <p class="text-sm font-medium text-slate-500">
            Scheduled for <%= Date.parse(params[:date]).strftime('%A, %B %-d') %>
          </p>
        <% else %>
          <p class="text-sm font-medium text-slate-500">Create a new training activity</p>
        <% end %>
      </header>

      <section class="card">
        <div class="card-section">
          <%= render "form", activity: @activity %>
        </div>
        <div class="card-section border-t border-slate-200 flex justify-between items-center">
          <%= link_to "Back to this plan", @plan, class: "text-blue-600 transition-colors font-bold underline text-lg" %>
        </div>
      </section>
    </div>
</div>
