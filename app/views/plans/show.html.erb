<div data-controller="activity-modal plan-processor" 
     data-plan-processor-plan-id-value="<%= @plan.id %>" 
     data-plan-processor-status-value="<%= @plan.processing_status %>"
     class="app-container-wide">
  <div class="stack-lg">
    <% if notice.present? %>
      <p class="mb-4 inline-flex items-center rounded-md bg-green-50 px-3 py-2 text-sm font-medium text-green-700 ring-1 ring-inset ring-green-200" id="notice"><%= notice %></p>
    <% end %>

    <header class="text-center stack-sm">
      <h1 class="heading-page">Training Plan</h1>
    </header>

    <section class="card w-full max-w-4xl mx-auto">
      <div class="card-section stack-md">
        <%= link_to "Back to plans", plans_path, class: "text-blue-600 transition-colors font-bold underline text-lg" %>
        <div class="flex flex-col md:flex-row md:justify-between gap-6">
          <div class="metric flex-1 md:text-left">
            <span class="metric-label">Plan Length</span>
            <span class="metric-value"><%= @plan.length %> Weeks</span>
          </div>
          <div class="metric flex-1 md:text-right">
            <span class="metric-label">Race Date</span>
            <span class="metric-value"><%= @plan.race_date.strftime("%B %d, %Y") %></span>
          </div>
        </div>

        <div class="divider"></div>

        <!-- Processing Spinner -->
        <div data-plan-processor-target="processing_indicator" class="<%= @plan.processing_status.in?(%w[queued processing]) ? '' : 'hidden' %>">
          <div class="text-center py-12">
            <div class="inline-flex items-center space-x-3">
              <svg class="animate-spin -ml-1 mr-3 h-6 w-6 text-blue-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
              <div class="text-gray-700">
                <div class="font-medium" data-plan-processor-target="status_text">Processing your training plan photos...</div>
                <div class="text-sm text-gray-500 mt-1">This may take a few minutes. Your workouts will appear automatically when ready.</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Training Calendar -->
        <div data-plan-processor-target="calendar" class="<%= @plan.processing_status.in?(%w[queued processing]) ? 'hidden' : '' %>" data-controller="week-selector">
          <h2 class="heading-section mb-6">Training Calendar</h2>
          
          <% training_start_date = (@plan.race_date - @plan.length.weeks).beginning_of_week %>
          <% activities = Activity.where(plan_id: @plan.id).index_by { |a| a.start_date_local.to_date } %>
          <% all_activities = Activity.where(plan_id: @plan.id) %>
          
          <!-- Week Selector -->
          <div class="mb-6">
            <select class="form-input w-full" 
                    data-week-selector-target="selector" 
                    data-action="change->week-selector#selectorChanged">
              <% @plan.length.times do |week| %>
                <% week_start = training_start_date + (week * 7) %>
                <% week_total = calculate_week_total_miles_from_collection(all_activities, week_start) %>
                <option value="<%= week %>" <%= 'selected' if week == 0 %>>
                  <%= week_start.strftime("%b %d") %> - <%= (week_start + 6.days).strftime("%b %d") %>
                </option>
              <% end %>
            </select>
          </div>

          <!-- Training Days -->
          <% @plan.length.times do |week| %>
            <% week_start = training_start_date + (week * 7) %>
            <% week_total = calculate_week_total_miles_from_collection(all_activities, week_start) %>
            <div class="week-view space-y-4 <%= 'hidden' unless week == 0 %>" 
                 data-week="<%= week %>" 
                 data-week-selector-target="week">
              
              <!-- Week Header -->
              <div class="flex items-center justify-between p-3 bg-slate-50 rounded-lg">
                <h3 class="text-sm font-semibold text-slate-700">Week <%= week + 1 %></h3>
                <div class="text-sm font-medium text-slate-600">
                  <span class="text-xs text-slate-500">Total:</span> 
                  <%= sprintf("%.1f", week_total) %> mi
                </div>
              </div>
              
              <!-- Draggable Day List -->
              <div class="space-y-3">
                <% 7.times do |day| %>
                  <% current_date = week_start + day.days %>
                  <div class="day-container">
                    <!-- Fixed Day Header -->
                    <div class="flex items-center justify-between mb-2 px-1">
                      <div>
                        <div class="text-base font-semibold text-slate-900"><%= current_date.strftime('%A') %></div>
                        <div class="text-xs text-slate-500"><%= current_date.strftime('%b %-d') %></div>
                      </div>
                      <button type="button"
                              class="calendar-edit-btn"
                              onclick="window.location.href='/activities/new?date=<%= current_date.to_s %>&plan_id=<%= @plan.id %>'">
                        Add
                      </button>
                    </div>
                    
                    <!-- Droppable Workout Area -->
                    <div class="min-h-[80px] rounded-lg border-2 border-dashed border-slate-200 bg-slate-50/50 p-3 transition-colors hover:border-slate-300 hover:bg-slate-100/50"
                         data-controller="drag" 
                         data-drag-target="container"
                         data-day="<%= current_date.to_s %>">
                      
                      <% day_activities = activities.select { |date, activity| date == current_date } %>
                      <% if day_activities.any? %>
                        <% day_activities.each do |date, activity| %>
                          <% original_date = activity.start_date_local.to_date %>
                          <div class="relative pl-8 pr-4 py-3 rounded-lg border border-slate-200 bg-white shadow-sm hover:shadow transition group mb-2 last:mb-0" 
                               data-drag-target="item" 
                               data-activity-cell 
                               data-date="<%= current_date.to_s %>"
                               data-activity-id="<%= activity.id %>"
                               data-original-date="<%= original_date.to_s %>">
                            
                            <!-- Drag Handle -->
                            <div class="absolute left-2 top-1/2 transform -translate-y-1/2 flex flex-col gap-1 p-1 text-slate-400 hover:text-slate-600 cursor-grab active:cursor-grabbing select-none opacity-70 group-hover:opacity-100 transition">
                              <div class="w-1 h-1 rounded-full bg-current"></div>
                              <div class="w-1 h-1 rounded-full bg-current"></div>
                              <div class="w-1 h-1 rounded-full bg-current"></div>
                              <div class="w-1 h-1 rounded-full bg-current"></div>
                              <div class="w-1 h-1 rounded-full bg-current"></div>
                            </div>
                            
                            <!-- Workout Content -->
                            <div class="flex items-start justify-between mb-2">
                              <div class="flex-1">
                                <div class="flex items-center gap-2 mb-1">
                                  <span class="badge-accent"><%= activity.distance %> mi</span>
                                  <% if original_date != current_date %>
                                    <span class="text-xs text-amber-600 bg-amber-50 px-2 py-1 rounded-full">
                                      Originally <%= original_date.strftime('%A') %>
                                    </span>
                                  <% end %>
                                </div>
                                <p class="text-xs text-slate-700 leading-snug"><%= activity.description %></p>
                              </div>
                              <button type="button"
                                      class="calendar-edit-btn ml-2"
                                      onclick="window.location.href='/activities/<%= activity.id %>/edit'">
                                Edit
                              </button>
                            </div>
                          </div>
                        <% end %>
                      <% else %>
                        <div class="text-center py-4 text-slate-400">
                          <div class="text-[10px] mt-1">Drag workouts here or Add</div>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
      <section class="card-section flex flex-col md:flex-row md:items-center gap-4">
        <div class="flex flex-wrap gap-2 md:ml-auto">
          <%= link_to "Edit plan", edit_plan_path(@plan), class: "btn-soft" %>
          <% if @plan.custom? %>
            <%= link_to "Edit workouts", edit_workouts_plan_path(@plan), class: "btn-outline" %>
          <% end %>
          <%= button_to "Delete plan", @plan, method: :delete,
              class: "btn-danger",
              form: { data: { turbo_confirm: "Are you sure you want to delete this plan?" } } %>
        </div>
      </section>
    </section>
  </div>
</div>